# Vercel Environment Variables
# Copy this to .env.local for local testing
# Set these in Vercel Dashboard for production

# API URL - Leave empty for local dev (uses proxy), set to Railway URL for production builds
VITE_API_URL=

# Example for production:
# VITE_API_URL=https://sanunjara-api.up.railway.app
